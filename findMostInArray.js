//Q Defind a function that take an array of string, and returns the most occurring in that array
//NOTE Beginner approach
const arr = ['aa', 'bb', 'cc', 'aa', 'aa'];
function mostCommonString(arrStr) {
  //create an empty object store the key-value pair
}

console.log(mostCommonString(arr));
//NOTE advance approach

var a = 10;
{
  a;
  var a = -10;
  a;
}

let b = a;
{
  let b = -20;
}
b;
b = 25;
b;
